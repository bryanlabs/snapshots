// Graz chain configuration
// For version 0.3.3, we don't need to explicitly define chains
export const grazConfig = {
  // Default chain ID for Cosmos Hub
  defaultChain: "cosmoshub-4",
  // Disable WalletConnect to avoid iframe issues
  walletConnect: undefined,
};